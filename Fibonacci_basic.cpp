#include<stdio.h>

typedef unsigned long long ull; // 코드를 깔끔하게 쓰기 위한 전처리   
ull fibonacci (ull n);

int main() {
	int n;
	printf("n번째 자연수를 입력하세요: ");
	scanf("%d", &n);
}

ull fibonacci (ull n) {
	 
}
